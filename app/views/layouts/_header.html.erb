<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "reservation booker", root_path, class: "brand" %>
      <ul class="nav pull-right">
		    <% if user_signed_in? %>
          <li><%= link_to "find restaurants", search_restaurants_path %></li>
          <li><%= link_to "view reservation requests", user_path(current_user) %></li>
		      <li><%= link_to "sign out", destroy_session_path(:user), method: :delete %></li>
        <% else %>
			    <li><%= link_to "home", new_user_session_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>